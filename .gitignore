# Rust build output
/target

# Built binaries (users build from source)
*.exe

# Bundled software-OpenGL runtime DLLs (Windows) — not source
libgallium_wgl.dll
opengl32.dll

# Editor / tooling
.claude/
.vscode/
.idea/

# Test media / encoder output — never commit videos
*.mkv
*.mp4
*.avi
*.mov
reencoded/

# Release artifacts / built binaries
/dist/
/fast6t
*.zip
*.tar.gz
